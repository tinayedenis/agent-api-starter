# Server
PORT=8787
HOST=0.0.0.0

# Path for JSON persistence (created automatically)
DATA_FILE=./data/store.json

# Seeded demo API key (Bearer). Credits granted on first boot if missing.
DEMO_API_KEY=agt_demo_sk_live_7f3a9c2e1b8d4e6f
DEMO_CREDITS=100

# Simple in-memory rate limit (requests per window per key)
RATE_LIMIT_MAX=60
RATE_LIMIT_WINDOW_MS=60000
